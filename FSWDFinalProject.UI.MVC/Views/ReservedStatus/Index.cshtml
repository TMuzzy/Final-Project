@model IEnumerable<FSWDFinalProject.DATA.EF.ReservedStatu>

@{
    ViewBag.Title = "Reservation Statuses";
}

<h2 class="padme"></h2>

<p>

</p>
<div class="container col-md-10 border1" style="margin-bottom: 1.5em;">
    <h2 style="padding-top: .5em;">@ViewBag.Title @Html.ActionLink("Add New Status", "Create", "", new { @class = "btn btn-primary" })</h2>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StatusName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.StatusName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ReservedStatusID }, new { @class = "btn btn-primary" })
                    @Html.ActionLink("Details", "Details", new { id = item.ReservedStatusID }, new { @class = "btn btn-secondary" })
                    @Html.ActionLink("Delete", "Delete", new { id = item.ReservedStatusID }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }

    </table>
</div>